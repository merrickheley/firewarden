extends layout


block content
    nav(class="navbar navbar-default navbar-fixed-top")
        div(class="container")
            div(class="navbar-header")
                button(type="button", class="navbar-toggle collapsed",data-toggle="collapse", data-target="#navbar", aria-expanded="false", aria-controls="navbar")
                    span(class="sr-only") Toggle navigation
                    span(class="icon-bar")
                    span(class="icon-bar")
                    span(class="icon-bar")
                a(class="navbar-brand", href="#") FireWarden
            div(id="navbar", class="collapse navbar-collapse")
                ul(class="nav navbar-nav navbar-left")
                    li
                        a(href="#") Home
                    li
                        a(href="#about") About
                    li
                        a(href="#contact") Contact
                ul(class="nav navbar-nav navbar-right")
                    button(class="btn navbar-btn" onclick='deleteMarkers();') Clear Markers
                    button(id="btnSubmit" class="btn navbar-btn disabled" onclick='submitMarkers();') Submit Markers

    div(id="main", class="container-fluid")
        div(id="map")
        div(id="add_form")
            button(id="dispatchEngine",class="btn btn-danger dispatch-btn") Dispatch Fire Engine
    footer(class="footer")
        div(class="container")
            div(class="row")
                div(class="col-md-6 col-md-offset-3")
                    input(id="timeline" type="text")
