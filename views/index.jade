extends layout


block content
    nav(class="navbar navbar-inverse navbar-fixed-top")
        div(class="container")
            div(class="navbar-header")
                button(type="button", class="navbar-toggle collapsed",data-toggle="collapse", data-target="#navbar", aria-expanded="false", aria-controls="navbar")
                    span(class="sr-only") Toggle navigation
                    span(class="icon-bar")
                    span(class="icon-bar")
                    span(class="icon-bar")
                a(class="navbar-brand", href="#") FireWarden
            div(id="navbar", class="collapse navbar-collapse")
                ul(class="nav navbar-nav navbar-left")
                    li
                        a(href="#") Home
                    li
                        a(href="#about") About
                    li
                        a(href="#contact") Contact
                ul(class="nav navbar-nav navbar-right")
                    button(class="btn navbar-btn" onclick='deleteMarkers();') Clear Markers
                    button(id="btnSubmit" class="btn navbar-btn disabled" onclick='submitMarkers();') Submit Markers
                    button(class="btn btn-danger navbar-btn" onclick='route();') Route

    div(id="main", class="container")
      div(id="map-container")
        div(id="map")
    
    footer(class="footer")
        div(class="container")
            input(id="timeline" type="text")
